{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "dependencies": {
    "cors": "^2.8.4",
    "firebase": "4.2.0",
    "firebase-admin": "~4.2.1",
    "firebase-functions": "^0.5.7"
  },
  "devDependencies": {
    "typescript": "^2.2.2"
  },
  "private": true,
  "scripts": {
    "deploy" : "cd .. && firebase deploy --only functions:evaluate",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
