{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "dependencies": {
    "cors": "2.8.4",
    "firebase": "4.6.1",
    "firebase-admin": "5.5.0",
    "firebase-functions": "0.7.3"
  },
  "devDependencies": {
    "typescript": "^2.2.2"
  },
  "private": true,
  "scripts": {
    "deploy": "cd .. && firebase deploy --only functions:playerStatus",
    "build": "tsc --skipLibCheck",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
